interface SessionConfigureProps {}

/**
 * Show the session configure view to the user.
 */
export default function SessionConfigure({}:SessionConfigureProps) {
    return <p>Configure Session</p>;
}
